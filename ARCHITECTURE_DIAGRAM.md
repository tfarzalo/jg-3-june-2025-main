# File Manager Architecture & Data Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  File Manager Component (FileManager.tsx)               │  │
│  │                                                           │  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌──────────┐   │  │
│  │  │  Folder │  │  File   │  │  Edit   │  │  Delete  │   │  │
│  │  │  Icon   │  │  Icon   │  │  (✏️)   │  │   (🗑️)   │   │  │
│  │  └─────────┘  └─────────┘  └─────────┘  └──────────┘   │  │
│  │                                                           │  │
│  │  Language: [EN] [ES]   ?lang=es                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                      TRANSLATION LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  translations = {                                               │
│    en: { fileManager: 'File Manager', rename: 'Rename', ... }  │
│    es: { fileManager: 'Gestor de Archivos', rename: 'Renombrar'} │
│  }                                                              │
│                                                                 │
│  t = translations[language]  // Selected based on URL param    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                        BUSINESS LOGIC                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  handleRename(item):                                            │
│  ├─ If folder: supabase.rpc('rename_folder', {...})           │
│  └─ If file: supabase.from('files').update(...)               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                       DATABASE LAYER                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  rename_folder(folder_id, new_name):                           │
│  ├─ Get folder details                                         │
│  ├─ Generate new paths (display + storage)                     │
│  ├─ Update folder record                                       │
│  ├─ Update all child files (recursive)                         │
│  ├─ Update all child folders (recursive)                       │
│  └─ Return summary JSON                                        │
│                                                                 │
│  Files Table:                                                   │
│  ├─ id (UUID)                                                  │
│  ├─ name (TEXT) ← Updated                                      │
│  ├─ display_path (TEXT) ← Updated (with spaces)               │
│  ├─ storage_path (TEXT) ← Updated (with underscores)          │
│  ├─ folder_id (UUID) ← Foreign key maintained                 │
│  └─ path (TEXT) ← Updated for compatibility                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Rename Flow Diagram

```
┌─────────────┐
│   USER      │
│ Clicks ✏️   │
└──────┬──────┘
       │
       ↓
┌──────────────────┐
│  Rename Modal    │
│  Opens           │
│  [Input Field]   │
│  [Rename Button] │
└──────┬───────────┘
       │
       │ User enters "Death Star"
       │ Clicks "Rename"
       ↓
┌───────────────────────────┐
│  handleRename() function  │
│                           │
│  Is it a folder?          │
├───────────┬───────────────┤
│    Yes    │      No       │
│           │               │
│    ↓      │      ↓        │
│  RPC      │   Simple      │
│  Call     │   UPDATE      │
└─────┬─────┴───────┬───────┘
      │             │
      ↓             ↓
┌─────────────────────────────────┐
│   rename_folder Function        │
│   (SQL/PostgreSQL)              │
│                                 │
│ 1. Get old folder details       │
│    - name: "death_star"         │
│    - display_path: "death_star" │
│    - storage_path: "death_star" │
│                                 │
│ 2. Calculate new paths          │
│    - new_name: "Death Star"     │
│    - new_display: "Death Star"  │
│    - new_storage: "death_star"  │
│                                 │
│ 3. Update folder record         │
│    SET name = "Death Star"      │
│    SET display_path = ...       │
│    SET storage_path = ...       │
│                                 │
│ 4. Update child files           │
│    WITH RECURSIVE ...           │
│    UPDATE files SET...          │
│                                 │
│ 5. Update nested folders        │
│    WITH RECURSIVE ...           │
│    UPDATE files SET...          │
│                                 │
│ 6. Return JSON summary          │
│    { success: true,             │
│      updated_files: 5,          │
│      updated_folders: 2 }       │
└──────────────┬──────────────────┘
               │
               ↓
┌──────────────────────────────────┐
│  Frontend receives response      │
│                                  │
│  - Close modal                   │
│  - Clear input                   │
│  - Refresh file list             │
│  - Show success (implicit)       │
└──────────────┬───────────────────┘
               │
               ↓
┌──────────────────────────────────┐
│  UI Updates                      │
│                                  │
│  Before: 📁 death_star           │
│  After:  📁 Death Star           │
│                                  │
│  All child files maintain        │
│  correct references              │
└──────────────────────────────────┘
```

## Language Selection Flow

```
┌───────────────────┐
│  User Navigates   │
│  to File Manager  │
└─────────┬─────────┘
          │
          ↓
┌───────────────────────────────┐
│  getLanguage() function       │
│                               │
│  1. Check URL param           │
│     ?lang=es or ?lang=en      │
│                               │
│  2. If not found, check       │
│     browser language          │
│     navigator.language        │
│                               │
│  3. Default to 'en'           │
└─────────┬─────────────────────┘
          │
          ↓
┌───────────────────────────────┐
│  useState(language)           │
│  t = translations[language]   │
└─────────┬─────────────────────┘
          │
          ↓
┌───────────────────────────────────────┐
│  Render UI with translations          │
│                                       │
│  English:                Spanish:     │
│  - File Manager         - Gestor...  │
│  - Rename              - Renombrar   │
│  - Delete              - Eliminar    │
│  - Upload Files        - Subir...    │
└───────────────────────────────────────┘
```

## Path Handling Strategy

```
User Input: "Death Star"
              │
              ↓
┌─────────────────────────────────┐
│  sanitize_for_storage()         │
│  (SQL function)                 │
│                                 │
│  "Death Star" → "death_star"    │
│  - Replace spaces with _        │
│  - Lowercase                    │
│  - Remove special chars         │
└────────┬────────────────────────┘
         │
         ↓
┌──────────────────────────────────────────┐
│  Database Storage:                       │
│                                          │
│  name: "Death Star"  ← User-friendly    │
│  display_path: "properties/123/         │
│                Death Star/before"        │
│  storage_path: "properties/123/         │
│                death_star/before"        │
│                                          │
│  Frontend shows: display_path            │
│  Storage uses: storage_path              │
└──────────────────────────────────────────┘
```

## Data Integrity Chain

```
Rename Parent Folder: "Property A" → "Property A Updated"

Parent Folder
├─ name: "Property A Updated"
├─ display_path: "Property A Updated"
└─ storage_path: "property_a_updated"
    │
    ├─ Child Folder 1: "Jobs"
    │  ├─ display_path: "Property A Updated/Jobs"  ← Auto-updated
    │  └─ storage_path: "property_a_updated/jobs"  ← Auto-updated
    │      │
    │      └─ File 1: "image.jpg"
    │         ├─ path: "property_a_updated/jobs/image.jpg"     ← Auto-updated
    │         ├─ display_path: "Property A Updated/Jobs/..."   ← Auto-updated
    │         └─ storage_path: "property_a_updated/jobs/..."   ← Auto-updated
    │
    └─ Child Folder 2: "Documents"
       ├─ display_path: "Property A Updated/Documents"  ← Auto-updated
       └─ storage_path: "property_a_updated/documents"  ← Auto-updated
           │
           └─ File 2: "contract.pdf"
              ├─ path: "property_a_updated/documents/contract.pdf"     ← Auto-updated
              ├─ display_path: "Property A Updated/Documents/..."      ← Auto-updated
              └─ storage_path: "property_a_updated/documents/..."      ← Auto-updated

✅ All references maintained
✅ No broken links
✅ Storage paths remain valid
✅ Display paths user-friendly
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│                     Work Order System                       │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│              ImageUpload Component                          │
│                                                             │
│  - Calls get_upload_folder() to get correct path           │
│  - Uploads to storage using storage_path                   │
│  - Saves to DB with folder_id reference                    │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│                   File Manager                              │
│                                                             │
│  - Displays files using display_path (user-friendly)       │
│  - Allows renaming via pencil icon                         │
│  - Updates all paths via rename_folder function            │
│  - Shows in English or Spanish based on URL                │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│                  Job Details View                           │
│                                                             │
│  - Fetches images using folder_id                          │
│  - Displays using public URLs from storage                 │
│  - Images show correctly even after folder rename          │
└─────────────────────────────────────────────────────────────┘

All components work together seamlessly!
```

## Security & Permissions

```
┌─────────────────────────────────────────┐
│           User Action                   │
│         (Click Rename)                  │
└──────────────┬──────────────────────────┘
               │
               ↓
┌──────────────────────────────────────────┐
│       Authentication Check               │
│       auth.uid() not null?               │
├──────────────┬───────────────────────────┤
│     Yes      │          No               │
│      ↓       │          ↓                │
│   Allow      │    Reject with error      │
└──────┬───────┴───────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────┐
│       RLS Policy Check                   │
│       Can user access this folder?       │
├──────────────┬───────────────────────────┤
│     Yes      │          No               │
│      ↓       │          ↓                │
│   Execute    │    SQL error returned     │
└──────┬───────┴───────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────┐
│      rename_folder Function              │
│      Executes with SECURITY DEFINER      │
│      Updates all related records         │
└──────────────────────────────────────────┘

✅ Only authenticated users can rename
✅ RLS policies enforced
✅ SQL injection prevented (parameterized)
✅ All updates atomic (transaction)
```

## This Visual Guide Shows:

1. **Component Architecture** - How FileManager fits into the system
2. **Rename Flow** - Step-by-step process from click to database update
3. **Language Selection** - How Spanish/English switching works
4. **Path Handling** - Display vs storage path strategy
5. **Data Integrity** - How recursive updates maintain consistency
6. **Integration Points** - How FileManager works with other components
7. **Security** - Authentication and authorization flow

Everything is connected, tested, and ready to deploy! 🚀
